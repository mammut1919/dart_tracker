import 'score_definition.dart';
import '../theme/app_colors.dart';

const defaultScores = [
  ScoreDefinition(score: 180, color: AppColors.score180),
  ScoreDefinition(score: 171, color: AppColors.score171),
  ScoreDefinition(score: 162, color: AppColors.score162),
];

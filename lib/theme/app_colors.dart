import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  // Theme
  static const primary = Colors.green;

  // Scores
  static const score180 = Colors.green;
  static const score171 = Colors.orange;
  static const score162 = Color(0xFF0000FF);

  // Achievements
  static const highFinish = Colors.deepPurple;
  static const shortLeg = Colors.red;

  // UI
  static const delete = Colors.red;
  static const tooltipBackground = Color(0xFFF5F5F5);
}
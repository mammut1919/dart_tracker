class ChartConstants {
  const ChartConstants._();

  static const chartHeight = 250.0;
  static const padding = 16.0;
  static const axisReservedSize = 34.0;
  static const lineWidth = 3.0;
  static const animationDuration =
      Duration(milliseconds: 350);
}